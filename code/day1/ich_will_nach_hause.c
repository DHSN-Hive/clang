#include <stdio.h> // WIll be inserted into this document when compiling!

int main(void) {
  printf("Hello, World!"); // imported from stdio.h
  return 1;                // 0 == success; x > 0 == error
}
